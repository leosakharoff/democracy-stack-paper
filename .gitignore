# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc

# Build intermediate (the renamed final copy is tracked)
main.pdf

# Scrape cache (firecrawl outputs)
.firecrawl/

# Bibliography source material (third-party PDFs / search results)
refs/
