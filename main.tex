\documentclass[11pt,a4paper]{article}

% --- Language and typography ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[htt]{hyphenat}  % allow hyphenation in \texttt

% --- Page layout ---
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{setspace}
\onehalfspacing
\usepackage{parskip}

% --- Tables and figures ---
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{float}     % [H] specifier
\usepackage{placeins}  % \FloatBarrier
\usepackage[font={small,sf}, labelfont={bf,sf}, labelsep=colon, skip=8pt, margin=1em]{caption}

% --- Lists, quotes, and graphics ---
\usepackage{enumitem}
\usepackage{csquotes}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning, fit, backgrounds, calc}
\usepackage{fontawesome5}

% Fork-divergence quadrant tints, matched to figure 1
\definecolor{quadgreen}{HTML}{D8ECD8}
\definecolor{quadred}{HTML}{F5D9DC}
\definecolor{quadgray}{HTML}{E9ECEF}
\definecolor{quadtan}{HTML}{FBEACC}

% --- Code listings ---
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\bfseries,
  commentstyle=\itshape\color{gray},
  frame=single,
  framerule=0.4pt,
  rulecolor=\color{gray!50},
  backgroundcolor=\color{gray!3},
  xleftmargin=1em,
  aboveskip=0.8em,
  belowskip=0.8em,
  breaklines=true,
}

% --- Citations and links ---
\usepackage[authoryear]{natbib}
\bibliographystyle{plainnat}

\usepackage{url}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!60!black,
  citecolor=blue!60!black,
  urlcolor=blue!60!black
}

% --- No default title ---
\begin{document}

% ============================================================
% TITLE PAGE
% ============================================================
\begin{titlepage}
\newgeometry{margin=3cm}

\begin{flushleft}
\includegraphics[width=3.2cm]{figures/itu_logo.png}
\end{flushleft}

\vspace{1.2cm}

\begin{flushleft}
{\fontsize{11}{13}\selectfont\textcolor{gray}{\textsf{IT University of Copenhagen}}}
\end{flushleft}

\vspace{2.5cm}

\begin{flushleft}
{\fontsize{26}{32}\selectfont\bfseries\sffamily Building the Democracy Stack}\\[12pt]
{\fontsize{15}{20}\selectfont\sffamily\textcolor{gray!80!black}{What Software Architecture Decisions Matter\\for Democratic Digital Infrastructure?}}
\end{flushleft}

\vspace{1.8cm}

{\color{black}\rule{\textwidth}{0.8pt}}

\vspace{1cm}

\begin{flushleft}
\renewcommand{\arraystretch}{1.6}
\begin{tabular}{@{}ll}
\textsf{\textcolor{gray}{Author}}      & \textsf{Leo William Engholm Sakharov} \\
\textsf{\textcolor{gray}{Email}}       & \textsf{leos@itu.dk} \\
\textsf{\textcolor{gray}{Programme}}   & \textsf{MSc Software Design} \\
\textsf{\textcolor{gray}{Course}}      & \textsf{Research Project (7.5 ECTS)} \\
\textsf{\textcolor{gray}{STADS code}}  & \textsf{KIREPRO1PE} \\
\textsf{\textcolor{gray}{Supervisor}}  & \textsf{Konstantinos Manikas} \\
\textsf{\textcolor{gray}{Date}}        & \textsf{15 May 2026} \\
\end{tabular}
\end{flushleft}

\vfill

\begin{flushleft}
{\footnotesize\textsf{\textcolor{gray}{IT University of Copenhagen $\cdot$ Rued Langgaards Vej 7 $\cdot$ 2300 Copenhagen S}}}
\end{flushleft}

\restoregeometry
\end{titlepage}
% ============================================================
% ABSTRACT
% ============================================================
\begin{abstract}
Civic technology platforms have been building digital democratic infrastructure for over a decade --- software for participatory budgeting, policy proposals, and citizen consultations. European policy is starting to call for the same thing, under names like the Digital Services Act, the European Digital Identity framework, IDEA's ``democracy stack'', and CEPS's case for European digital public infrastructure. But policy talks about what this infrastructure should do, not how it should be built. The platforms themselves have rarely been analysed through software architecture and software ecosystem (SECO) methods. This paper proposes a small analytical framework for thinking about software architecture decisions in democratic digital infrastructure. The framework has three parts: four quality attributes that connect architecture to democratic capacities (modifiability, interoperability, configurability, and transparency and auditability); Christensen et al.'s three-structures lens, where ecosystem health depends on the fit between organisational, business, and software structures; and a set of architectural trade-offs that follow from the four attributes. The paper also develops \emph{fork-divergence shape}, a way to read niche creation from the shape of a platform's fork population.

The framework is applied to a comparative case study of Decidim and Consul, two civic tech platforms with shared origins in Spain's 15M movement and opposite architectural paths. The application uses architectural recovery, evolutionary analysis, and a fork-divergence study across all public forks of both platforms (1{,}030 for Consul, 452 for Decidim). Decidim's modular software fits its coordinating Association and consultancy-based funding. Consul's monolithic software needs coordination that its small Foundation and shoestring funding cannot provide. The fork-divergence shapes invert at the originator: Madrid (Consul) sits in an abandoned 12{,}725-commit silo, Barcelona (Decidim) tracks upstream with zero local commits. On each platform the diagnostic reads a different niche --- deployment fragmentation for fork-based Consul, contributor flow for plugin-based Decidim --- depending on the platform's extension model. Quality attributes where Decidim and Consul behave similarly --- security, accessibility, sovereignty --- are left for future work.
\end{abstract}

\newpage
\tableofcontents
\newpage

% ============================================================
% 1. INTRODUCTION
% ============================================================
\section{Introduction}

European policy is starting to call for digital infrastructure for democracy --- the Digital Services Act \citep{eu2022dsa}, the European Digital Identity framework \citep{eu2024eid}, IDEA's ``democracy stack'' \citep{idea2026democracy}, and CEPS's case for European Digital Public Infrastructure \citep{ceps2025building}. These proposals say what the infrastructure should do, not how it should be built. Civic technology platforms have been building this kind of software for over a decade --- Decidim across hundreds of institutions in 32 countries \citep{barandiaran2024decidim}, Consul across over 250 cities and organisations \citep{ntari2025consul} --- yet how their architectures shaped their ecosystems remains under-analysed.

This paper asks: \emph{what software architecture decisions matter for democratic digital infrastructure, and how can they be analysed through software architecture and software ecosystem methods?}

The methods exist across software architecture, SECO, STS/platform studies, and fork-mining research (Section~\ref{sec:background}), but have rarely been combined to analyse civic tech: \citet{manikas2016revisiting}'s 2016 review of 231 SECO studies found no work on civic or commons-based platforms.

We propose a small analytical framework combining four quality attributes (modifiability, interoperability, configurability, transparency and auditability), \citet{christensen2014analysis}'s three-structures lens, and a set of trade-offs. We apply it to Decidim and Consul --- two platforms with shared origins in Spain's 15M movement (2015--2016) but opposite architectural paths (Decidim modular, 27 Ruby engines + coordinating Association; Consul monolithic, deployers fork + small Foundation coordinates from the outside) --- using architectural recovery, commit history analysis, and a fork-divergence study across 1{,}030 Consul and 452 Decidim public forks. From the application we surface \emph{fork-divergence shape}, a population-shape niche-creation diagnostic whose reading depends on the extension model. Decidim and Consul are first worked cases, not a full validation across the field.

% ============================================================
% 2. BACKGROUND
% ============================================================
\section{Background}\label{sec:background}

Reasoning about democratic digital infrastructure needs three literatures: software architecture (for quality attributes and trade-offs), software ecosystems (for the organisational, business, and technical structures around a platform), and architecture-as-governance (for how software architectures make some forms of action, coordination, and control easier than others).

\vspace{0.8em}
\begin{center}
\begin{tikzpicture}[
  font=\sffamily\small,
  every node/.style={align=center},
  src/.style={circle, fill=black, inner sep=2pt},
  dst/.style={circle, fill=black, inner sep=2.5pt},
]
\node[src, label={above:Software\\architecture}]      (sa) at (-3.6, 0.8) {};
\node[src, label={above:Software\\ecosystems}]        (se) at ( 0.0, 0.8) {};
\node[src, label={above:Architecture\\as governance}] (ag) at ( 3.6, 0.8) {};
\node[draw, rounded corners, fill=gray!10, inner sep=5pt, font=\sffamily\small\bfseries] (p) at ( 0.0,-1.0) {Analytical framework};
\draw[gray!70, thick] (sa) -- (p);
\draw[gray!70, thick] (se) -- (p);
\draw[gray!70, thick] (ag) -- (p);
\end{tikzpicture}\\[2pt]
{\footnotesize\itshape Three literatures combined into one analytical framework.}
\end{center}

\subsection{Software Architecture Quality Attributes}\label{sec:sa}

\citet[p.~4]{bass2021software} call software architecture ``the set of structures needed to reason about a system.'' Architecture decisions affect a system's properties over its lifetime through quality attributes: measurable properties like modifiability, performance, security, and so on. Quality attributes are useful here because they let us evaluate architecture without reducing it to implementation details. They allow us to ask not only what a platform contains, but what kinds of change, inspection, integration, and adaptation its structure makes easier or harder.

In commercial software, quality attributes are usually optimised for business value. For democratic digital infrastructure the goal is different: which architectural properties support democratic participation, public legitimacy, and institutional adaptability?

We focus on four quality attributes.

\paragraph{Modifiability.} How easily the system can be changed. Civic tech platforms have to adapt to communities with different rules, languages, and participatory traditions; modifiable platforms can do this without forcing each deployer to maintain a separate fork.

\paragraph{Interoperability.} How well the system exchanges data and services with others. This matters because no single platform will serve all democratic functions, so platforms need to compose.

\paragraph{Configurability.} The platform's support for configuring varied participation processes (proposals, voting, deliberation, accountability) without changing the source code. PolicyKit \citep{zhang2020policykit} prototyped programmable governance for online communities, a close analogue of this property at the governance-rule layer.

\paragraph{Transparency and auditability.} How much of the platform's operation can be inspected by relevant observers (transparency), and how reliably those observations can be used to verify and contest outcomes (auditability). Democratic legitimacy depends partly on both, although technical auditability does not by itself guarantee that citizens can meaningfully use these mechanisms in practice.

Other quality attributes (security, accessibility, sovereignty, performance) matter for democratic infrastructure too. We do not treat them as primary axes here and return to this scoping in Section~\ref{sec:discussion}.

\subsection{Software Ecosystems}\label{sec:seco}

SECO research broadens the unit of analysis from the software to the wider system around it. \citet{manikas2013systematic} define a software ecosystem (SECO) as ``the interaction of a set of actors on top of a common technological platform, which results in a number of software solutions or services.'' The concept extends beyond a single product to the organisations, developers, and users that develop around a shared platform.

\citet{christensen2014analysis} propose that ecosystems can be analysed through three interacting structures: an \emph{organisational} structure (who participates, in what roles, how they coordinate), a \emph{business} structure (who pays, who captures value, what incentives flow), and a \emph{software} structure (the platform architecture, extension mechanisms, APIs). Their argument is that ecosystems thrive when these three structures fit together and struggle when they do not. We use this lens in Section~\ref{sec:application} to compare Decidim and Consul.

We also draw on \citet{iansiti2004strategy}'s ecosystem actor classification (keystones, dominators, niche creators) and \citet{jansen2014measuring}'s ecosystem health dimensions (productivity, robustness, niche creation). In this paper, these concepts are treated as analytical lenses rather than direct measurements of democratic value.

\citet{manikas2016revisiting}'s systematic review of 231 SECO studies found none on commons-based, civic, or democratic platforms. \citet{knutas2020local} proposed applying OSSECO methods to civic tech but did not carry out the empirical work. \citet{palacin2024configurations} study Decidim's modularity from a human-computer interaction perspective, but do not analyse the architecture itself as part of a software ecosystem.

\subsection{Architecture as Governance}\label{sec:archgov}

A long tradition in science and technology studies (STS) and platform studies has established that software architectures are not politically neutral: they distribute possibilities, making some forms of participation, oversight, and control easier than others \citep{winner1980artifacts, lessig2006code, denardis2014global, schneider2024governable}. This reading is analytical rather than deterministic --- architecture does not by itself cause political outcomes. \citet{schneider2021modular}'s \emph{Modular Politics} moves toward design, proposing four principles (modularity, expressiveness, portability, interoperability) for a governance layer in online communities. Two of these principles overlap with QAs developed in this paper (modularity with modifiability, interoperability with interoperability), and we revisit this resonance in Section~\ref{sec:framework}. These ideas are directly relevant to civic technology platforms, but have rarely been combined with software architecture and SECO methods.

% ============================================================
% 3. METHOD
% ============================================================
\section{Method}\label{sec:method}

This paper is a comparative case study of two civic technology platforms, Decidim and Consul, following the case-study guidelines for software engineering of \citet{runeson2009guidelines}. The empirical work uses two methods: architectural recovery \citep{ducasse2009software, garcia2013comparative} for reconstructing each platform's software structure from primary sources, and Mining Software Repositories \citep{hassan2008road} for the commit-history, contributor-concentration, logical-coupling, and fork-divergence analyses. The framework in Section~\ref{sec:framework} is built from existing software architecture and SECO literature; the application in Section~\ref{sec:application} demonstrates it on the two cases but does not validate it across the wider civic tech field. Evaluation across more cases is left for future work.

\paragraph{Case selection.}
We selected Decidim and Consul as the comparative case for two reasons. First, both have publicly developed codebases, active deployments, and enough documentation for architectural recovery. Second, the two platforms share an origin in Spain's 15M movement in 2015--2016 but took opposite architectural paths: Decidim went modular (27 Rails engines, gem-based extension); Consul stayed monolithic (single Rails app, fork-based extension). Both platforms emerged from Spain's 15M movement in 2015--2016, so the era, country, and political project are held constant; the architectural divergence is the contrast we want to study. This is theoretical case selection: the cases are chosen for their analytical contrast, not statistical representativeness. Other civic tech platforms (Polis, Loomio, CitizenLab, vTaiwan, Adhocracy) are not analysed; a single two-platform comparison is enough to demonstrate the framework at this stage, but not enough to generalise across civic tech as a whole.

\paragraph{Architectural recovery.}
For each platform we recover the software structure from primary sources: codebase organisation, gemspec files, extension mechanisms, and documentation. For Decidim, a script parses all 27 of Decidim's own gemspecs at a pinned snapshot, extracts every \texttt{add\_dependency} call, classifies each as internal or external, and identifies \texttt{Decidim.register\_component} and \texttt{Decidim.register\_participatory\_space} calls. The engine dependency graph in Section~\ref{sec:application} is recovered from this data, not asserted from documentation. Consul's recovery is structural: a Rails monolith with a file-override extension mechanism. The recovery captures dependency structure and extension mechanisms, not runtime behaviour or deployment-specific customisations.

\paragraph{Evolutionary analysis.}
For both platforms we extract commit metadata with \texttt{git log --no-merges} on partial clones. Bot authors (Dependabot, Crowdin, Weblate, GitHub Actions, named release bots) are filtered by email and name patterns. We compute total human commits, unique authors, top-three and top-ten contributor share, Gini coefficient (a measure of contributor concentration: 0 means commits are evenly distributed across authors, 1 means one author makes them all), and the per-year commit timeline; for Decidim we also extract per-engine commit counts. We use these as indicators of ecosystem activity and concentration \citep{hassan2008road, manikas2013systematic, jansen2014measuring}, not as direct measures of ecosystem health or democratic value.

\paragraph{Logical coupling.}
Following \citet{gall1998detection}, we measure which Decidim engines change together. For each non-merge non-bot commit we identify the engines touched and compute Jaccard similarity (commits touching both / commits touching either) for each engine pair. Mega-commits touching more than eight engines are excluded as refactor or release commits that would otherwise dominate the signal. Logical coupling is computed only for Decidim, since Consul's monolithic structure has no engine boundary to test against. This tests whether the modular structure shows up in how commits actually fall, not only in the declared dependencies.

\paragraph{Fork divergence.}
For both platforms we use the GitHub REST API to enumerate all public forks of the upstream repository (\texttt{consuldemocracy/consuldemocracy}, formerly \texttt{consul/consul}; and \texttt{decidim/decidim}) and call the compare endpoint for each, returning commits ahead and behind upstream's default branch (\texttt{master} for Consul, \texttt{develop} for Decidim), drawing on the fork-mining literature \citep{zhou2020forks, wang2023forkentropy, businge2022reuse}. Each fork is classified as \emph{modified} or \emph{unmodified} (commits ahead) and \emph{active} or \emph{dormant} (pushed in the last three years; this is a methodological choice rather than a fixed convention). The analysis covers public forks only and may miss private production forks. The two platforms have different extension models: Consul deployers customise by forking the application, while Decidim deployers install the gem in their own Rails application and extend through plugins. As a result, the same diagnostic reads different ecosystem niches in each case --- deployment fragmentation for Consul, contributor flow for Decidim --- a methodological point we return to in the cross-case synthesis and discussion.

\paragraph{Three-structures application.}
For each platform we describe the three structures from \citet{christensen2014analysis} --- organisational, business, software --- using primary documentation (Decidim Association governance documents, MetaDecidim assembly records, Consul Democracy Foundation website, OSOR case studies) and the empirical material from architectural recovery and evolutionary analysis. We then assess alignment or misalignment between the three structures. This assessment is interpretive: it asks whether the software structure fits the organisational and business reality around it, and whether the organisational and business structures can sustain the architectural costs. Specifically, alignment evidence includes whether the organisational structure has roles to use the software's extension mechanisms, whether the business structure can sustain the architectural maintenance costs, and whether contribution flows match the paths the software allows. It does not establish causal proof.

\paragraph{Quality-attribute assessment.}
The four quality attributes are assessed qualitatively as low, moderate, or high. Table~\ref{tab:qa-rubric} gives the criteria per QA per level. The ratings are based on architectural mechanisms and evidence from the repository and documentation; they are structured judgements, not measurements.

\begin{table}[!htbp]
\centering
\caption{Quality-attribute rating rubric: criteria for low, moderate, and high ratings.}
\label{tab:qa-rubric}
\renewcommand{\arraystretch}{1.3}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.5cm}XXX}
\toprule
\textbf{Quality attribute} & \textbf{Low} & \textbf{Moderate} & \textbf{High} \\
\midrule
Modifiability &
\cellcolor{quadred} Extension requires core code changes or a deployment fork &
\cellcolor{quadtan} Plugin hooks exist; some adaptation still requires forking &
\cellcolor{quadgreen} Modules extend the platform; no core changes or forks needed \\
\midrule
Interoperability &
\cellcolor{quadred} No public API or shared data formats &
\cellcolor{quadtan} Partial API exposure or limited standard-format support &
\cellcolor{quadgreen} Stable public API; standard formats supported (e.g., ActivityPub, OpenAPI) \\
\midrule
Configurability &
\cellcolor{quadred} Participation processes fixed in code &
\cellcolor{quadtan} Some processes configurable; limited coverage &
\cellcolor{quadgreen} Varied processes composable via settings (proposals, voting, deliberation, accountability) \\
\midrule
Transparency and auditability &
\cellcolor{quadred} Operations not logged beyond standard application logs &
\cellcolor{quadtan} Some inspection mechanisms; not first-class architectural concerns &
\cellcolor{quadgreen} First-class logging, versioning, permission-gated visibility, public inspection \\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Data sources.}
Primary sources are the public GitHub repositories at pinned snapshots: \texttt{decidim/decidim} at \texttt{be39c244} (2026-05-12) and \texttt{consuldemocracy/consuldemocracy} at \texttt{3c3b5c4a} (2026-05-11). Secondary sources include \citet{barandiaran2024decidim} on Decidim's sociotechnical design, the OSOR case study on Consul's European deployments \citep{linaker2025consul}, the NTARI fork report \citep{ntari2025consul}, and the Codegram architecture blog on early Decidim design \citep{codegram-decidim}. Reproducibility scripts (\texttt{scripts/} directory: gemspec parsing, commit metadata extraction, fork-divergence analysis) and findings logs are publicly available at \url{https://github.com/leosakharoff/democracy-stack-paper}.

% ============================================================
% 4. FRAMEWORK
% ============================================================
\section{Framework}\label{sec:framework}

\subsection{The Three Components}\label{sec:framework-three}

The three components below offer overlapping but complementary views of a platform: the quality attributes look at the software structure itself, the three-structures lens locates the software within its organisational and business context, and the architectural trade-offs name the choices that follow from the QA mapping.

\subsubsection{Quality Attributes Mapped to Democratic Capacities}\label{sec:framework-qas}

Each of the four quality attributes introduced in Section~\ref{sec:sa} connects an architecture choice to a democratic capacity the platform can support. Each one also pairs with an architectural cost the platform has to pay if it chooses that direction. Table~\ref{tab:qa-mapping} collects the mappings.

\begin{table}[!htbp]
\centering
\caption{Quality attributes mapped to democratic capacities and the architectural costs they impose.}
\label{tab:qa-mapping}
\renewcommand{\arraystretch}{1.3}
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Quality attribute} & \textbf{Democratic capacity} & \textbf{Architectural cost} \\
\midrule
Modifiability & Pluralism: diverse communities can adapt the platform & Sustainability burden of $N$ composable parts \\
\midrule
Interoperability & Composability: platforms can join wider stacks & Reduced local autonomy under shared standards \\
\midrule
Configurability & Procedural pluralism: participation can be organised in different ways & Configuration surface complexity; maintenance overhead \\
\midrule
Transparency and auditability & Legitimacy, accountability, and contestability can be supported through inspection and verification & Audit infrastructure: logs, versioning, permissions, and API discipline \\
\bottomrule
\end{tabularx}
\end{table}

The mappings are analytical, not causal proofs: they identify democratic capacities each attribute can support when matched by appropriate organisational and institutional conditions, and a different corpus or normative starting point would land on different mappings. Modifiability and configurability address different audiences --- developers and administrators respectively --- and both matter for democratic infrastructure.

\subsubsection{The Three-Structures Lens}\label{sec:framework-ts}

The QAs above describe properties of the software structure alone. \citet{christensen2014analysis}'s three-structures framework gives us a way to think about how the software structure relates to the broader ecosystem.

The three structures (organisational, business, software) are introduced in Section~\ref{sec:seco}.

The lens locates the QAs in their organisational and business context: modifiability matters for pluralism only if contributors can use the extension mechanisms; transparency matters for legitimacy only if actors are able to inspect and act on what they see. It also names a failure mode beyond bad architecture --- \emph{misalignment} between the software structure and the organisational and business reality around it. A modular architecture without a coordinating body may produce fragmentation rather than pluralism; a modifiable platform without sustained funding may produce decay rather than adaptation.

\subsubsection{Architectural Trade-offs}\label{sec:framework-tradeoffs}

The four QAs cannot all be optimised without cost. Each one pairs with an architectural burden (Table~\ref{tab:qa-mapping}), and choosing one direction means accepting that burden elsewhere in the ecosystem.

Three pairings are particularly visible in civic tech and form the framework's architectural trade-offs:

\paragraph{Modifiability against sustainability burden.} Modular architectures let many deployers --- municipalities, NGOs, civic-tech consultancies --- adapt the platform; the cost is maintaining many parts. Monolithic architectures can be easier to maintain centrally; the cost is that adaptation often requires forking or direct source modification unless other extension mechanisms are provided.

\paragraph{Interoperability against reduced autonomy.} Interoperable platforms compose into wider stacks; the cost is constraints on local choice under shared standards. Stand-alone platforms preserve local autonomy; the cost is isolation.

\paragraph{Configurability against maintenance overhead.} Highly configurable platforms support many different participation processes without code changes; the cost is configuration surface complexity and the maintenance of options across versions. Less configurable platforms are simpler; the cost is that adapting to a new process may require code changes.

Each trade-off is not only an engineering choice but also a governance choice. The architecture makes some forms of adaptation, coordination, and oversight easier than others, whether or not the builders frame the decision politically. The framework makes these choices explicit.

\subsection{Fork-Divergence Shape: A Niche-Creation Diagnostic}\label{sec:framework-fd}

Fork-mining research has developed several lenses on fork divergence: evolution-pattern typologies of fork--upstream pairs derived from commit-history graphs \citep{zhou2020forks}, scalar entropy measures of fork diversity in modified files \citep{wang2023forkentropy}, and longitudinal studies of code propagation between mainlines and divergent forks across software ecosystems \citep{businge2022reuse}. \citet{wang2023forkentropy} motivate their work with a premise close to ours --- that ``merely having a large number of forks does not necessarily imply a productive and healthy OSS project'' --- and \citet{businge2022reuse} report little code propagation between mainlines and divergent forks across the Android, .NET, and JavaScript ecosystems --- and the fork$\rightarrow$mainline direction is rarer still --- a pattern we replicate in the civic tech setting for Consul. None of this work has been applied to civic tech platforms, and none connects fork behaviour to SECO health dimensions explicitly.

Standard SECO niche-creation metrics, in turn, count contributions but do not by themselves distinguish healthy ecosystem extension from silo formation. We adapt the fork-mining literature into a population-shape diagnostic --- \emph{fork-divergence shape} --- that reads the distribution of public forks across ahead-of-upstream and behind-upstream states, pairs with \citet{jansen2014measuring}'s niche-creation dimension, and reads as a three-structures alignment check for civic tech platforms where forks are a relevant extension mechanism.

Operationally, each fork can be located in one of four states, defined by whether it has local commits ahead of upstream and whether it has fallen behind upstream. Table~\ref{tab:fork-states} names the four cells and the interpretation we attach to each.

\begin{table}[!htbp]
\centering
\caption{The four fork-divergence states. Cell colours match the quadrants of Figure~\ref{fig:consul-divergence}, so the table reads as a legend for the figure.}
\label{tab:fork-states}
\renewcommand{\arraystretch}{1.5}
\begin{tabularx}{\textwidth}{l X X}
\toprule
 & \textbf{Behind upstream = 0} & \textbf{Behind upstream > 0} \\
\midrule
\textbf{Ahead upstream > 0} &
\cellcolor{quadgreen}\emph{Ahead-only} --- healthy extension; local work kept current with upstream &
\cellcolor{quadred}\emph{Ahead-and-behind} --- diverged silo; local work drifted from upstream \\
\midrule
\textbf{Ahead upstream = 0} &
\cellcolor{quadgray}\emph{In-sync} --- copy that mirrors upstream &
\cellcolor{quadtan}\emph{Behind-only} --- dormant copy, fallen behind \\
\bottomrule
\end{tabularx}
\end{table}

The shape of the fork population, not the total count, is the diagnostic. Healthy ecosystem extension should populate the ahead-only region: deployers keep up with upstream and add their own work, producing changes that upstream can in principle absorb. Silo formation should populate the behind-only and ahead-and-behind regions: either dormant copies fall behind, or active deployments accumulate local changes while also missing upstream changes. We apply this to Consul in Section~\ref{sec:application-consul} and find an empty ahead-only region, which we interpret as a fragmentation signal.

% ============================================================
% 5. APPLICATION
% ============================================================
\section{Application: Decidim and Consul}\label{sec:application}

\subsection{Decidim}\label{sec:application-decidim}

Decidim is a modular monolith built with Ruby on Rails. The codebase at \url{github.com/decidim/decidim} contains 27 engines packaged as standalone Ruby gems at SHA \texttt{be39c244} (2026-05-12).

\paragraph{Software structure.}
The 27 engines fall into four groups: \texttt{decidim-core} as the foundational dependency, three infrastructure engines (\texttt{admin}, \texttt{api}, \texttt{system}), three shared-service engines (\texttt{comments}, \texttt{forms}, \texttt{verifications}), four participatory spaces (\texttt{processes}, \texttt{assemblies}, \texttt{conferences}, \texttt{initiatives}), and 13 components that attach to those spaces. Every engine except \texttt{decidim-core} declares \texttt{add\_dependency "decidim-core"} in its gemspec, matching a hub-and-spoke pattern.

However, 13 of the 27 engines have runtime dependencies on other Decidim engines besides core: \texttt{decidim-comments} has 6 user-facing dependents and \texttt{decidim-forms} has 5, making them Layer-2 hubs of the platform (Figure~\ref{fig:dependency}).

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{figures/decidim_dependency_graph.pdf}
\caption{Decidim engine dependency graph at SHA \texttt{be39c244} (2026-05-12), recovered from the 27 gemspec files. The grey bar above \texttt{decidim-core} stands in for the 26 universal core dependencies. The dashed arrows are cross-engine runtime dependencies that bypass core; the most-depended-on are \texttt{comments} (6 dependents) and \texttt{forms} (5). Three developer-tooling engines (\texttt{design}, \texttt{dev}, \texttt{generators}) are omitted.}
\label{fig:dependency}
\end{figure}

The key architectural pattern is the space/component matrix: almost any component can attach to almost any space, creating an $N \times M$ combinatorial structure implemented through a polymorphic database association in core. \citet[p.~82]{barandiaran2024decidim} call this the ``furniture'' model: administrators mix and match components with spaces without writing code. Components are registered through a manifest API (\texttt{Decidim.register\_component}); community modules use the same interface as the official ones.

To examine whether modular design is reflected in modular evolution, we applied Gall et al.'s logical coupling analysis \citep{gall1998detection} to the commit history. Of 5{,}517 commits (excluding mega-commits per Section~\ref{sec:method}), $64\%$ stay within a single engine. The engine architecture provides change isolation at the repository level, though not runtime independence. The remaining cross-engine coupling is concentrated in the four participatory spaces (Jaccard up to $0.43$), which share infrastructure code despite having no gemspec dependencies between them (Figure~\ref{fig:coupling}).

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\textwidth]{figures/decidim_coupling.pdf}
\caption{Logical coupling between Decidim engines, computed as Jaccard similarity over the project's commit history at SHA \texttt{be39c244}. The four participatory spaces (centre block) co-change despite having no gemspec dependencies between them. The shared services (\texttt{comments}, \texttt{forms}, \texttt{verifications}) are largely isolated --- their gemspec-dependents call them as APIs without co-changing.}
\label{fig:coupling}
\end{figure}

\paragraph{Organisational structure.}
The Decidim Free Software Association was founded on 16 February 2019 in a community assembly that approved its statutes. The Association holds the source code and trademark, transferred from Barcelona City Council in 2017. Its governance includes a General Assembly as sovereign body and four standing committees: Coordination, Research, Decidim Politics, and Public Institutions. The Public Institutions Committee brings Barcelona City Council, the Generalitat de Catalunya, the Barcelona Provincial Council, Localret, and other public bodies into the coordination structure \citep{barandiaran2024decidim}.

MetaDecidim is the community layer: a public Decidim instance at meta.decidim.org through which both governance and product work happen. The Decidim Social Contract, binding on members, commits signatories to AGPLv3 licensing, transparency, and ``equal opportunities for participatory objects'' \citep{barandiaran2024decidim} --- the no-ranking-bias rule that surfaces in the software as a randomised default sort. The Association runs a small technical office (three employees per Barandiaran et al.) that handles release management and code review. Module ownership is distributed across Codegram engineers, Association staff, and the wider contributor base; 183 unique authors are visible in the commit log.

\paragraph{Business structure.}
The Association is funded through annual transfers from Barcelona City Council and the Generalitat de Catalunya under a formal collaboration agreement, with additional support from the Consorci Localret and the Barcelona Provincial Council \citep{barandiaran2024decidim}. Around this institutional core, a network of consultancies funds itself through municipal contracts: Codegram (Barcelona), Octree (Geneva), Open Source Politics (Paris), and smaller partners. The economic loop is: municipalities procure deployments from these consultancies; the consultancies are paid for installation, customisation, hosting, and bespoke modules; some of the work can flow back upstream as gem-based community modules or core pull requests. Decidim documents more than 80 community-developed gems, a visible sign of this return flow.

\paragraph{Fork divergence.}
Of 452 enumerated forks of \texttt{decidim/decidim}, 444 had upstream comparison data. The distribution is concentrated in the behind-only quadrant: 399 forks ($90\%$) have no local commits but have fallen behind upstream as Decidim ships on \texttt{develop}. Forty-five forks sit in the ahead-and-behind quadrant; none in ahead-only or in-sync. The most-divergent fork is 221 commits ahead --- a Spanish-language rebrand of the documentation website, two orders of magnitude below Consul's deepest silo (12{,}725 commits ahead).

The originator pattern inverts. Madrid (Consul originator) abandoned a 12{,}725-commit fork in 2023. Barcelona (Decidim originator, \texttt{AjuntamentdeBarcelona/decidim}) sits at zero commits ahead and forty-six behind, with a push timestamp from the day before the snapshot --- the originator is tracking upstream. Codegram, the consultancy that built Decidim, also sits at zero commits ahead. AjuntamentManacor and other municipal forks show the same pattern. Figure~\ref{fig:decidim-divergence} plots all 444 comparable forks on identical axes to Figure~\ref{fig:consul-divergence}, so the contrast reads directly.

This is not a claim that Decidim has a healthier deployment ecosystem in raw numbers. The documented installation path is \texttt{gem install decidim; decidim my\_app}, which produces a new Rails application living in its own repository rather than as a fork of upstream. Most Decidim deployments are therefore invisible to fork-tree analysis. The 444 fork population captures contributors with PRs in flight (typically single-digit ahead counts), learners reading the codebase (zero ahead, accumulating behind), and a small minority of deep-customisation deployers. The plugin path has displaced the deployment fork --- not the small ahead-and-behind cluster --- and that is the empirical signal.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/decidim_fork_divergence.pdf}
\caption{Decidim forks plotted by commits ahead of upstream \texttt{develop} against commits behind (snapshot 2026-05-14), on the same axes as Figure~\ref{fig:consul-divergence}. The ahead-only region is empty, as for Consul. The mass sits in the behind-only quadrant (399 dormant or learner copies, $90\%$). The ahead-and-behind cluster is small in both count (45 forks) and magnitude (top fork at 221 commits ahead, two orders of magnitude below Consul's Madrid). Barcelona, the Decidim originator, sits at zero ahead and forty-six behind --- the inverse of Madrid's silo. Of 452 enumerated forks, 444 had upstream comparison data.}
\label{fig:decidim-divergence}
\end{figure}

\paragraph{Quality attribute profile.}
\emph{Modifiability} is high. The engine architecture lets deployers add, remove, or replace components without touching core code; customisation happens through modules, not deployment forks. The 80+ community modules are evidence that this modifiability is used in practice.

\emph{Interoperability} is moderate. A GraphQL API exposes most participatory content, and the project's integration choices (OpenStreetMap, Matomo, Jitsi) are deliberately open-source. There are no cross-platform standards for civic tech that would let Decidim compose with other platforms at the data layer, so interoperability remains less developed than modifiability or configurability in this case.

\emph{Configurability} is high. The 4 spaces and 13 components produce many possible default configurations, each customisable through component-level settings without code changes. \citet{palacin2024configurations} document 25 distinct production configurations across studied deployments.

\emph{Transparency and auditability} is high architecturally. Decidim ships an immutable \texttt{ActionLog} model with a four-level visibility enum (private, admin-only, public-only, all) and an organisation scope. The \texttt{Traceable} concern adds PaperTrail-based version history to every changeable model, so per-proposal version diffs are available. Permission-gated controllers enforce admin-log access. The GraphQL API is designed to expose public participatory content while not exposing individual votes or admin actions. Sorting is deterministic and seeded for reproducibility within a session. These mechanisms make auditability a first-class architectural concern, although they do not by themselves prove that non-technical citizens can meaningfully use the audit surfaces in practice.

Figure~\ref{fig:decidim-engine-activity} plots per-engine commits in the 12 months before the snapshot. All 24 user-facing engines see activity, with three new engines (\texttt{ai}, \texttt{demographics}, \texttt{collaborative\_texts}) accumulating their first hundreds. The modular architecture is therefore matched by visible maintenance activity across the engine set; no user-facing engine is dormant in this snapshot.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\textwidth]{figures/decidim_engine_activity.pdf}
\caption{Decidim commits per user-facing engine in the 12 months before the snapshot (2025-05-12 to 2026-05-12). Bar values are commits in the period; the annotations show all-time commits for context. Three developer-tooling engines (\texttt{design}, \texttt{dev}, \texttt{generators}) are excluded, matching Figure~\ref{fig:dependency}.}
\label{fig:decidim-engine-activity}
\end{figure}

\paragraph{Three-structures alignment.}
The three structures align. The modular software lets external contributors ship gems without negotiating commit access to the core repository; the Association coordinates contributions and runs keystone functions such as releases, code review, and integration; the consultancy network has a business model around incremental deployment and module work; and the Public Institutions Committee channels public-body requirements back upstream. When Barcelona pays for accessibility work, that work can land in \texttt{decidim-core} and become available to other municipalities, which is the commons logic Barandiaran et al. describe. The 80+ community modules and the steady commit rate after 2019 are visible indicators of this alignment.


\subsection{Consul}\label{sec:application-consul}

The original repository has since been renamed from \texttt{consul/consul} to \texttt{consuldemocracy/consuldemocracy}. Recovery is pinned to SHA \texttt{3c3b5c4a} (2026-05-11).

\paragraph{Software structure.}
Consul is a single Rails application: no gem separation, no engine isolation. Features (proposals, debates, budgets, legislation, voting, polls) share the same codebase, the same \texttt{ApplicationController}, and one \texttt{config/routes.rb}. Models live in \texttt{app/models/} with functional subdirectories but no module-level namespace boundaries enforced. All tables sit in one schema. There is no \texttt{ARCHITECTURE.md}, and the technical documentation covers installation but not architecture.

Customisation uses a custom shadow-folder pattern: to change how a budget investment works, a deployer creates a file at the same path under \texttt{app/models/custom/} that overrides the original. This reduces some file-level conflicts but does not provide module isolation. The Consul Democracy installation guide names creating a fork as the first step, making deployment forks part of the expected customisation path.

\paragraph{Organisational structure.}
The Consul Democracy Foundation was founded in 2019 as a Dutch non-profit \citep{linaker2025consul}. It was set up specifically because Madrid's institutional backing for Consul collapsed after the May 2019 municipal election. The Foundation is very small: a Director, a Network Coordinator, a Tech Coordinator, and a three-person board. Crucially, the Foundation does not employ developers. \citet{linaker2025consul} report that maintenance relies on ``a technical core group dominated by Spanish service suppliers dating to Consul's early days.'' The community is held together through annual ConsulCon events, a Slack workspace of 700--800 users, and a two-tier Certified Tech Partner programme. There is no Decidim-style Social Contract, no Public Institutions Committee, and no MetaDecidim-equivalent space where deployers participate in governance.

\paragraph{Business structure.}
The Foundation is funded by a patchwork: a single municipal sponsor (the City of Munich at EUR~20{,}000 per year), EU project grants, private donations, and revenue contributions from Certified Tech Partners \citep{linaker2025consul}. The Foundation does not commission core development directly; it cannot afford to. Municipalities procure Consul deployments from certified suppliers, but the architecture means much customisation happens inside the deployer's own fork rather than as a returnable contribution. \citet{linaker2025consul} report that contributions ``come largely from certified service suppliers, with fewer contributions from local governments and NGOs'' and that ``many development features rarely merge back to the core codebase due to architectural barriers and fragmentation across installations.''

\paragraph{Fork divergence.}
We enumerated all forks of \texttt{consuldemocracy/consuldemocracy} via the GitHub API and computed each fork's divergence against upstream master. Of 1{,}030 enumerable forks, 1{,}026 had upstream comparison data. 746 ($73\%$) are unmodified copies that act more like bookmarks than forks; 280 ($27\%$) have at least one commit ahead --- approximately the ``around 250 deployments'' figure previous reports cite \citep{ntari2025consul}. The distribution within those 280 is heavily skewed: most have fewer than ten commits ahead, 39 have 100 or more, and only 5 have 1{,}000 or more. Activity has been declining: pushes peaked in 2019 and only $14\%$ of forks have been touched since the start of 2024 (Figure~\ref{fig:consul-forks}).

The most-diverged fork is \texttt{AyuntamientoMadrid/consul} --- Madrid itself, the city that built Consul. It sits 12{,}725 commits ahead of upstream and 11{,}540 behind, last pushed 2023-05-23. The originating city therefore maintained a separate copy that drifted far from upstream and was eventually abandoned. This does not prove that the architecture alone caused the divergence, but it shows that even the originator ended up using the fork pattern that the platform made easiest. Plotted in Figure~\ref{fig:consul-divergence}, three quadrants are populated and one is conspicuously empty: 280 ahead-and-behind silos (top-right), 745 behind-only dormant copies (bottom-right), 1 in-sync fork (bottom-left), and zero ahead-only forks (top-left). The empty top-left is the diagnostic signal --- no fork keeps up with upstream while also adding local work.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/consul_fork_divergence.pdf}
\caption{Consul forks plotted by commits ahead of upstream master against commits behind, on log scales (snapshot 2026-05-12). Each fork occupies one of four quadrants. The ahead-only region --- where healthy contributor forks would live --- is empty: no fork has added local commits while staying in sync with upstream. The mass sits in the behind-only region (745 dormant copies) and the ahead-and-behind region (280 diverged silos, including Madrid). Of 1{,}030 enumerated forks, 1{,}026 had upstream comparison data; the remaining four are excluded.}
\label{fig:consul-divergence}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{figures/consul_fork_activity.pdf}
\caption{Last-push year distribution for the 1{,}030 enumerable Consul forks (snapshot 2026-05-12). Each bar counts forks whose most recent push falls in that year. Activity peaks in 2019 and declines; only $14\%$ of forks have been touched since the start of 2024.}
\label{fig:consul-forks}
\end{figure}

\paragraph{Quality attribute profile.}
\emph{Modifiability} is low. Customisation is expected to happen through forks and shadow-folder overrides rather than through isolated modules. Shadow folders reduce file-level conflicts but do not remove logical coupling to the upstream codebase. The 1{,}030 forks are the visible signature of this extension model, although not every fork represents a production deployment.

\emph{Interoperability} is moderate. The public GraphQL API at \texttt{/graphql} exposes participatory content including individual vote records, but the data model is monolithic and there are no cross-platform standards.

\emph{Configurability} is moderate. Several components (proposals, debates, budgets, voting) can be enabled or disabled per deployment, with phase- and quorum-level settings. The configuration is less rich than Decidim's space/component matrix.

\emph{Transparency and auditability} is moderate. Consul uses the off-the-shelf \texttt{audited} gem to track per-record changes, but the UI exposes audit history only for Budget Investments. A separate \texttt{Activity} model logs five specific moderation verbs (hide, block, restore, valuate, email). Both surfaces are admin-only. There is no Decidim-style organisation-scoped \texttt{ActionLog} with public visibility options.

\paragraph{Three-structures misalignment.}
The three structures misalign. The problem is not monolithic architecture as such: a monolith can work well when it is matched by strong central maintenance and clear contribution pathways. The problem is the fit between Consul's monolithic extension model and its organisational and business structures. The software structure makes local adaptation easiest through forks; reintegrating that work would require a strong coordinating body with sustained development capacity; the small Foundation cannot provide that capacity; and the business structure does not reliably pay anyone to provide it. The architecture's costs therefore land on individual deployers, who absorb them by maintaining isolated forks. The misalignment shows up in the fork data: 1{,}030 forks, 750 unmodified copies, and 280 modified forks with limited evidence of upstream return.

Two pieces of evidence make this concrete. First, Madrid itself ended up in the same fork pattern as other deployers, even with the resources and motivation of the originating city. Second, the post-2019 collapse: when Ahora Madrid lost the mayoralty in May 2019, the upstream commit rate dropped roughly $80\%$ within a year. The Foundation, set up that same year, lacked core developers or significant funding; it could coordinate the ecosystem but not maintain the core. The claim is not that the monolith caused the misalignment, but that this monolithic extension model required a coordinating capacity that the organisational reality could not provide.


\subsection{Cross-case Synthesis}\label{sec:application-synthesis}

Decidim's contributor base is less concentrated than Consul's, and Consul's upstream activity collapsed in 2019. The commit history makes both patterns visible. Figure~\ref{fig:contributor-concentration} shows the concentration: Decidim has 7{,}078 human commits across 183 unique authors with a Gini coefficient of $0.85$ (top-three share $36\%$, top-ten $68\%$), against Consul's 186 unique authors with a Gini coefficient of $0.93$ (top-three $52\%$, top-ten $87\%$). These figures indicate contributor concentration in the repository, not the full distribution of influence in the wider ecosystem. Figure~\ref{fig:commit-timeline} shows the Consul cliff in 2019--2020 following Ahora Madrid's electoral loss in May 2019 \citep{linaker2025consul, barandiaran2024decidim}, alongside Decidim's steadier plateau after 2019.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{figures/contributor_concentration.pdf}
\caption{Share of human commits captured by the top three and top ten authors per repository. Decidim has a longer tail of unique contributors; Consul is more concentrated. Bot commits (Dependabot, Crowdin, etc.) are excluded.}
\label{fig:contributor-concentration}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{figures/commit_timeline.pdf}
\caption{Human commits per year for each repository (no merges, bots excluded). The Consul cliff in 2019--2020 follows Ahora Madrid's electoral loss in May 2019; Decidim shows a steadier plateau after 2019. The 2026 numbers are partial (cut-off at clone date).}
\label{fig:commit-timeline}
\end{figure}

\paragraph{Three-structures comparison.}

\begin{table}[!htbp]
\centering
\caption{Three-structures analysis \citep{christensen2014analysis} applied to Decidim and Consul.}
\label{tab:threestructure}
\renewcommand{\arraystretch}{1.3}
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Structure} & \textbf{Decidim} & \textbf{Consul} \\
\midrule
Software       & Modular: 27 engines, registry pattern, shared DB, gem-based extension & Monolithic: single Rails app, shadow folder pattern, fork-based extension \\
\midrule
Organisational & Decidim Association as keystone; MetaDecidim community; Social Contract; Public Institutions Committee & Consul Democracy Foundation (6 people, no developers); informal supplier network; weak coordination \\
\midrule
Business       & Catalan institutional funding + anchor consultancies (Codegram, Octree, Open Source Politics) + Public Institutions Committee feedback loop & EUR~20{,}000/yr from Munich + EU grants + supplier certification revenue; no anchor consultancy \\
\bottomrule
\end{tabularx}
\end{table}

Decidim's structures appear aligned. The modular software affords incremental contribution; the Association coordinates it; the consultancies help sustain it. Consul's structures appear misaligned. The monolithic extension model requires a strong coordinator that the Foundation cannot afford to be, and deployers absorb the misalignment by maintaining forks.

\paragraph{Trade-off positioning.}
The two platforms sit at opposite ends of the modifiability trade-off identified in Section~\ref{sec:framework-tradeoffs}. Decidim chose high modifiability and pays the sustainability burden of a 27-engine codebase maintained by a small technical office. Consul chose lower modifiability and pays the cost in fork fragmentation: 280 modified forks with little visible upstream return. On interoperability, both are open-source and have GraphQL APIs but neither pursues cross-platform civic tech standards; the trade-off does not differentiate them strongly and remains under-tested in this two-case application. On configurability, Decidim's space/component matrix is high and Consul's per-feature settings are moderate; the cost on the Decidim side is the configuration complexity \citet{palacin2024configurations} document.

\paragraph{Transparency.}
Transparency and auditability differ sharply across the two at the architectural level. Decidim treats auditability as a first-class architectural concern through the \texttt{ActionLog}/\texttt{Traceable} infrastructure; Consul treats it more narrowly as a moderation oversight feature implemented via off-the-shelf gems. This differentiation supports including transparency and auditability as a primary axis of the framework, while leaving open the separate question of how usable these mechanisms are for non-technical citizens.
\FloatBarrier

% ============================================================
% 6. DISCUSSION
% ============================================================
\section{Discussion}\label{sec:discussion}

\subsection{Architecture as Governance in Practice}\label{sec:disc-gov}

The Decidim/Consul comparison shows how architectural decisions participate in governance in civic technology. Decidim's modular architecture did not only make the code flexible; it let communities and service providers shape the platform without first negotiating commit access to the core repository. An organisation can build a module, publish it as a gem, and have it work alongside the official engines. This is close to what \citet{schneider2021modular} call ``modular politics'': composable governance primitives implemented as software. The 80+ community modules are one visible artefact of this politics in action.

Consul's monolithic extension model made a different path easier. The fork-and-shadow pattern made isolated local adaptation more straightforward than upstream contribution: deployers could either accept the platform as-is or maintain a separate fork. \citet{schneider2024governable} argues that platform defaults encode governance; in Consul's case, the default extension path pulled local adaptation toward forked copies rather than shared modules, as visible at scale in the Madrid divergence (Section~\ref{sec:application-consul}).

Read through \citet{iansiti2004strategy}'s actor roles, Decidim's Association operates as a keystone --- maintaining shared infrastructure while enabling third-party modules --- and the 80+ community modules are the niche-creation that follows. Consul's Madrid team operated more like a dominator while institutional backing held: controlled codebase, modularisation rejected, no module mechanism for external contribution. When backing collapsed in 2019, no successor keystone emerged; deployers absorbed the vacuum by maintaining their own forks.

\subsection{What SECO Research Gains}\label{sec:disc-seco}

\citet{jansen2014measuring}'s SECO health dimensions --- productivity, robustness, niche creation --- do not by themselves distinguish ecosystem extension from silo formation. Consul's 2017--2019 peak generated thousands of commits and dozens of unique contributors: by productivity measures, the ecosystem looked active. Yet the extension model pushed adopters toward separate forks, and when Ahora Madrid lost the city in 2019 the upstream commit rate dropped roughly $80\%$. By fork-divergence measures, the ecosystem was already vulnerable to fragmentation.

Fork-divergence shape as a niche-creation diagnostic (Section~\ref{sec:framework-fd}) is one concrete methodological contribution this paper makes to SECO research, adapting the fork-mining literature \citep{zhou2020forks, wang2023forkentropy, businge2022reuse} into a population-shape reading paired with \citet{jansen2014measuring}'s niche-creation dimension for civic tech platforms. Figures~\ref{fig:consul-divergence} and~\ref{fig:decidim-divergence} plot the two cases on identical axes. Consul's quadrant pattern --- 280 ahead-and-behind silos and 745 behind-only dormant copies, with the ahead-only quadrant empty --- is a fragmentation signal: the deployer fork population extends away from upstream rather than around it. Decidim's shape is structurally different: the ahead-and-behind cluster is small in both count (45 forks) and magnitude (top fork at 221 commits ahead, two orders of magnitude below Consul's Madrid), and the originator sits at zero local divergence.

The three-structures framework of \citet{christensen2014analysis}, applied here to civic tech, also extends the SECO toolkit. The misalignment reading of Consul's fork fragmentation makes the structural argument concrete: not that the architecture was simply bad, but that the extension model required a coordinating capacity the organisational and business structures could not provide. Architecture and organisation diverged regardless of which way the causal arrow runs.

\subsection{Implications for the Democracy Stack}\label{sec:disc-stack}

International IDEA \citeyearpar{idea2026democracy} calls for a democracy stack but leaves the architectural decisions implicit. \citet{ceps2025building} advocates for interoperable European digital public infrastructure but does not engage the modifiability-against-sustainability trade-off that shapes whether interoperable infrastructure can actually be adapted by the communities it serves. Two lessons from our cases speak to these gaps.

\paragraph{Modularity as a condition for pluralistic adaptation.}
Trade-off 1 (modifiability against sustainability burden) makes the strongest case for modular architecture in a democracy stack. The Decidim/Consul comparison shows what can happen on either side. The trade-off is real: modular maintenance is harder than maintaining a smaller central codebase. But the alternative, when local adaptation is needed and no other extension mechanism exists, is fork fragmentation that upstream cannot easily absorb. For a democracy stack that must accommodate diverse institutional contexts, modularity appears to be a central architectural condition for pluralistic adaptation. The sustainability burden should therefore be treated not as an implementation inconvenience, but as a governance cost that must be deliberately funded and coordinated.

\paragraph{Transparency by design, not by audit.}
Decidim's transparency and auditability come from architectural decisions made early: a first-class \texttt{ActionLog}, the \texttt{Traceable} concern wired into changeable models, and a GraphQL API designed to expose public participatory content while protecting sensitive data. Consul's transparency mechanisms are narrower and implemented mainly through off-the-shelf audit and activity-tracking features. For a democracy stack that has to remain auditable across deployments and over time, transparency is difficult to add as a superficial feature after launch; it has to be treated as an architectural property. The cost --- audit infrastructure across models, permissions, visibility levels, and APIs --- is real, but without it the legitimacy of participatory outcomes is harder to verify.

\subsection{Limitations}\label{sec:evaluation}

The two-platform comparison is the framework's first worked application, not a validation across the field. Several limitations bound what we can claim.

\emph{Causality.} The architecture-as-governance reading is correlational. Barcelona and Madrid had different governance cultures and political trajectories; Madrid's Ahora Madrid government lost power in 2019, hurting Consul's institutional backing. Separating what the architecture caused from what politics caused would need interview research we did not do. The architecture and the organisational reality diverged in opposite directions, and the misalignment matters regardless of which way the causal arrow runs.

\emph{Single coder.} The architectural and ecosystem judgements were made by one author; a second coder could disagree on edge cases (which engine counts as ``shared service'', which fork counts as ``substantively modified''). Where possible we triangulated against published characterisations --- \citet{linaker2025consul} on Consul's organisational structure, \citet{barandiaran2024decidim} on Decidim's governance, the OSOR and NTARI reports on Consul deployments --- and noted disagreements explicitly.

\emph{Anglophone bias.} Almost all our sources are English; Decidim has substantial Spanish- and Catalan-language documentation, and the Consul fork landscape is heavily Spanish, Catalan, and Latin American. We likely missed material that would refine our reading.

\emph{Selection on the dependent variable.} We chose two platforms that exist; the civic tech graveyard --- platforms that never reached production or failed invisibly --- is invisible to this study. The trade-offs we identify are conditional on survival.

\emph{Architectural recovery limits.} Architectural recovery captures dependency structure and extension mechanisms, not runtime behaviour. Evolutionary metrics use email-based author identity, which conflates authors with multiple emails. Fork analysis covers public forks only and may miss private production deployments. Each of these is a real limit on what the methods can see.

% ============================================================
% 7. CONCLUSION
% ============================================================
\section{Conclusion}\label{sec:conclusion}

Decidim and Consul started from similar political and historical origins but developed into different software ecosystems. Decidim's modular software fits its coordinating Association and consultancy-based funding. Consul's monolithic extension model is less well matched to its small Foundation and shoestring funding, and this misalignment is visible in the platform's fork fragmentation. This paper makes sense of that contrast by bringing together three established literatures: software architecture quality attributes, \citet{christensen2014analysis}'s three-structures lens for software ecosystems, and the architecture-as-governance thesis from STS and platform studies. The contribution is not to invent those components, but to combine them into an analytical framework for civic technology platforms and demonstrate how the framework reads two worked cases. The main methodological contribution that emerged from the application is \emph{fork-divergence shape}: a diagnostic for whether a civic tech ecosystem extends around a shared upstream or fragments into diverged forks.

\paragraph{Future research.}
Two directions invite follow-on work. First, extending the framework to a wider set of platforms: CitizenLab for sovereignty under commercial SaaS, Loomio for worker-cooperative governance, and a federated platform for security and identity. Second, stakeholder interviews with Decidim deployers, Consul fork maintainers, and Association members would complement the repository-mining done here and address the causal limitations in Section~\ref{sec:evaluation}.

% ============================================================
% USE OF GENERATIVE AI
% ============================================================
\section*{Use of Generative AI}
\addcontentsline{toc}{section}{Use of Generative AI}

\textbf{Tool used:} Claude (Anthropic; Claude Sonnet 4.6 and Claude Opus 4.7), accessed via the Claude Code command-line interface.

\textbf{Use in the project:} Generative AI was used as an assistant during the research and writing process. It supported literature discovery, outlining and structural decisions, language refinement and copy editing, exploratory web research, and the drafting and debugging of Python and matplotlib scripts used to collect, process, and visualise repository data. The scripts and paper source are publicly available alongside the paper on GitHub (see Section~\ref{sec:method}, ``Data sources'').

\textbf{Author contribution and validation:} The author developed the research question, case selection, architectural framing, quality-attribute analysis, interpretation of findings, prose, and final argumentative structure. AI was used as a research and writing assistant --- supporting refinement of drafts, language editing, code review, and exploration of alternative framings --- with all AI-generated content reviewed, revised, tested, and validated by the author against primary sources, public documentation, and repository data. The analytical claims, figures, and conclusions presented in the paper are the author's own responsibility.

\textbf{Data, copyright, and confidentiality:} No confidential material or personal data was provided to the AI tool. The work was based on the author's notes, public documentation, public repositories, and publicly available sources.

All submitted text, figures, code, and analysis were reviewed, edited, and approved by the author.

% ============================================================
% REFERENCES
% ============================================================
\bibliography{references}

\end{document}
